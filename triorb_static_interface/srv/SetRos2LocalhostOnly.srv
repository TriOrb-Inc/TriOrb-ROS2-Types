# Persist ROS_LOCALHOST_ONLY as 0 or 1.
# Backend may write an environment variable and/or edit the DDS XML.
# 0 turns localhost-only off, 1 turns it on.
uint8 data
---
# True when the persist request completed successfully.
bool success
# Human-readable completion or error details.
string message
